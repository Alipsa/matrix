# Note In order to maintain compatibility for Java, Scala and Kotlin users
# We only use extensions to register (override) short notations so that the
# "regular" method still works as expected, thus enabling other JVM languages to use this library
moduleName=matrix-core
moduleVersion=1.0
# comma separated list of classes that overrides DefaultGroovyMethods (and create new ones if needed)
extensionClasses=se.alipsa.groovy.matrix.RowExtension,se.alipsa.groovy.matrix.ListToColumnConversionExtension
# staticExtensionClasses=support.StaticStringExtension