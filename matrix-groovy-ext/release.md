# Matrix-groovy-ext release history

## v0.1.1 2026-03-23
- Improved `sin()` and `cos()` accuracy for large angles by using higher-precision range reduction.
- Made `atan2(0, 0)` return `0` to match `Math.atan2`.
- Clarified GroovyDoc for internal precision constants and trigonometric implementations.

## v0.1.0 2026-01-30
Initial version
- Number extensions allowing for more idiomatic groovy code.
